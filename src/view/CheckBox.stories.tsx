import { CheckBox } from './CheckBox';

export default {
  title: 'View/CheckBox',
  component: CheckBox,
  tags: [ 'autodocs' ],
  argTypes: {},
};

export const Empty = () => {
  return (
    <CheckBox />
  );
}