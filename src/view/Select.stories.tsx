import { Select } from './Select';

export default {
  title: 'View/Select',
  component: Select,
  tags: [ 'autodocs' ],
  argTypes: {},
};

export const Closed = () => {
  return (
    <div>
      SELECT
    </div>
  );
}