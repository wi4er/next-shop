import { Input } from './Input';

export default {
  title: 'View/Input',
  component: Input,
  tags: [ 'autodocs' ],
  argTypes: {},
};

export const Empty = () => {
  return (
    <Input>

    </Input>
  );
}