import { RadioButton } from './RadioButton';

export default {
  title: 'View/RadioButton',
  component: RadioButton,
  tags: [ 'autodocs' ],
  argTypes: {},
};

export const Empty = () => {
  return (
    <div>
      Radio Button
    </div>
  )
}